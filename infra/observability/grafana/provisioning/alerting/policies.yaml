apiVersion: 1

policies:
  - orgId: 1
    # канал доставки задаётся одной переменной: ALERT_CHANNEL=telegram|email
    receiver: $ALERT_CHANNEL
    group_by: ['alertname']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
