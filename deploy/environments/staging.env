# Публичные переменные staging. Секретов здесь нет — они в GitHub Environment "staging".
NODE_ENV=production
LOG_LEVEL=info
APP_DOMAIN=
EMAIL_FROM=no-reply@localhost
EMAIL_PROVIDER=smtp
ALERT_CHANNEL=telegram
# PostgreSQL в контейнере
COMPOSE_PROFILES=local-db
