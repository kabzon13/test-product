# Публичные переменные production. Секретов здесь нет — они в GitHub Environment "production".
NODE_ENV=production
LOG_LEVEL=info
# Домен появился → вписать сюда (и Origin CA в секреты), затем деплой
APP_DOMAIN=
EMAIL_FROM=no-reply@localhost
EMAIL_PROVIDER=resend
ALERT_CHANNEL=telegram
